class KBREAKPOINT {
  KBREAKPOINT._();

  /// the length of [sm] in [double] is [640px].
  static const double sm = 640;

  /// the length of [md] in [double] is [768px].
  static const double md = 768;

  /// the length of [lg] in [double] is [1024px].
  static const double lg = 1024;

  /// the length of [xl] in [double] is [1280px].
  static const double xl = 1280;

  /// the length of [xxl] in [double] is [1536px].
  static const double xxl = 1536;
}
